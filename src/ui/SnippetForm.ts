import * as vscode from 'vscode';

export class SnippetForm {
  static show(_context: vscode.ExtensionContext, _code: string, _language: string): void {
    throw new Error('Not implemented yet — coming in Phase 4');
  }
}
