// Minimal vscode stub — only what the tested modules transitively need
export const EventEmitter = class {
  event = () => {};
  fire() {}
  dispose() {}
};
export const Uri = { joinPath: () => ({}) };
export const window = {};
export const workspace = {};
export const env = {};
export const commands = {};
export enum WebviewViewProvider {}
